# Database Configuration
POSTGRES_USER=aisec
POSTGRES_PASSWORD=change-this-secure-password
POSTGRES_DB=aisec_db
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=3000

# Database URL (used by Prisma)
DATABASE_URL=postgresql://aisec:change-this-secure-password@postgres:5432/aisec_db?schema=public

# JWT Secret (MUST CHANGE - minimum 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-this-min-32-characters

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@ai-sec.com

# Adminer (Database UI) Port
ADMINER_PORT=8080
